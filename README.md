# padctl

Linux userspace gamepad compatibility layer daemon.

Documentation: [padctl doc repo](../Dev_Vault/padctl/)

## Requirements

- Zig 0.15+
- libusb-1.0 (`libusb-1.0-devel` or `libusb-dev`)

## Build

```sh
zig build
```
