const std = @import("std");

// Stub: open TOML in $VISUAL/$EDITOR, validate on exit. Full impl in T9.
pub fn run(allocator: std.mem.Allocator, path: ?[]const u8) !void {
    _ = allocator;
    _ = path;
    return error.NotImplemented;
}
