const std = @import("std");

// Stub: load mapping, read hidraw input, print mapped events. Full impl in T9.
pub fn run(allocator: std.mem.Allocator, path: ?[]const u8) !void {
    _ = allocator;
    _ = path;
    return error.NotImplemented;
}
